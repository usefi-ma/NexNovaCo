@page "/dashboard/content/home"
@page "/dashboard/home/hero"
@inject NavigationManager Navigation

@* Both aliases retain the Dashboard folder's Admin authorization, including direct HTTP requests. *@
@code {
    protected override void OnInitialized() =>
        Navigation.NavigateTo("/dashboard/content/home/hero", replace: true);
}
