@page "/dashboard/content/services"
@inject NavigationManager Navigation
<PageTitle>Services | NexNovaCo Dashboard</PageTitle>
@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/dashboard/content/services/hero", replace: true);
}
